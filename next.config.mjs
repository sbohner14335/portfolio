/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "90ck8cgfuv.ufs.sh",
      },
    ],
  },
}

export default nextConfig
